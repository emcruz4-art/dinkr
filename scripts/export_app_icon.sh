#!/bin/bash
# Export AppIcon sizes from the SwiftUI view
# Run from project root: bash scripts/export_app_icon.sh
echo "AppIconView created — open AppIconPreview in Xcode to screenshot and export"
echo "Or use: xcrun simctl screenshot to capture from simulator"
